'use strict';

let React = require('react');
let ReactDOM = require('react-dom');

let Authorized = React.createClass({
	render: function() {
		return (
			<div>authorized</div>
		);
	}
});

module.exports = Authorized;
